<%= link_to bike_path(bike) do %>
  <div class="card-product mt-2">
    <%= cl_image_tag(bike.photo.key) %>
    <div class="card-product-infos">
      <h2><%= bike.name %></h2>
      <p>Current rental price: <%= bike.price %>€</p>
      <p>My bike is free to rent? <%= bike.active %></p>
    </div>
  </div>
<% end %>
